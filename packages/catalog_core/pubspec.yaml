name: catalog_core
description: Pure-Dart catalog core for cat(a)log — append-only entry log over SQLite with latest-wins projection.
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  archive: ^3.6.1
  crypto: ^3.0.3
  image: ^4.2.0
  path: ^1.9.0
  sqlite3: ^2.4.6

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
