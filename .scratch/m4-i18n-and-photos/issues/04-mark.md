# 04 — Marker tool

**What to build:** "Mark…" in the photo menu: drag an ellipse over one cat, save — the highlight is baked into a JPEG copy appended as a new photo (original stays). Shows everywhere a photo shows, including printed cards.

**Blocked by:** 03 — Crop (shares the photo-edit flow).

**Status:** done

- [x] Ellipse drag-to-draw over the photo, visible stroke on any background
- [x] Save bakes the mark into a copy; original kept
- [x] Unit test: ellipse baked into synthetic image bytes
